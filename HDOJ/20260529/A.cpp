#include <bits/stdc++.h>

using namespace std;

using LL = long long;

void solve() {
    LL a, b, n, m;
    cin >> a >> b >> n >> m;
    
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int T = 1;
    cin >> T;
    while (T --) solve();
}