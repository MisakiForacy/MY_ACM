#include <bits/stdc++.h>

#define x first 
#define y second 

#define siz(x) ((int)x.size())
#define all(x) begin(x),end(x)

using namespace std;
using LL = long long;

const LL inf = 1e15;

vector<LL> p2(60, 1);

void init() {
    for ()
}

void solve() {
    LL x, k;
    cin >> x >> k;
    vector<LL> a(x + 1);
    int p = 1;
    while (k) {

    }
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int T = 1;
    cin >> T;
    while (T --) solve();
}