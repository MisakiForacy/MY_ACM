#include <bits/stdc++.h>

#define x first 
#define y second 

#define siz(x) ((int)x.size())
#define all(x) begin(x),end(x)

using namespace std;
using LL = long long;

void solve() {
    int n, a, b, f = 0;
    cin >> n >> a >> b;
    if ((n & 1) || (a + b & 1)) {
        cout << "No\n";
        return;
    } else {
        
    }
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int T = 1;
    cin >> T;
     while (T --) solve();
}