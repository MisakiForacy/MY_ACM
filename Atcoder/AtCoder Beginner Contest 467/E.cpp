#include <bits/stdc++.h>

using namespace std;

using LL = long long;

void solve() {
    vector<LL> a(n + 1, 0), b(n, 0)
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int T = 1;
    cin >> T;
    while (T --) solve();
}