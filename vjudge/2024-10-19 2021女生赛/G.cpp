#include <bits/stdc++.h>
using namespace std;
using LL = long long;
int main(){
    int n,x,y;
    cin >> n;
    for (int i=0;i<n;i++){
        cin >> x >> y;
    }
    cout << fixed << setprecision(9) << 1.0 / n << '\n';
}