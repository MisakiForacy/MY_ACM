#include <bits/stdc++.h>
using namespace std;
using LL = long long;
int main(){
    int n;
    cin >> n;
    vector<int> a(n);
    vector<int> dp;
    for (int i=0;i<n;i++){
        cin >> a[i];
        auto id = lower_bound(dp.begin(),dp.end())
    }
}