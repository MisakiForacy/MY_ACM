#include <bits/stdc++.h>
using namespace std;
using LL = long long;

int main() {
    while (1) {
        // getchar();
        system("gen.exe > in");
        system("E.exe < in > Eout");
        system("EEE.exe < in > EEEout");
        int ok = system("spj.exe in Eout EEEout");
        if (ok != 0) break;
    }
}