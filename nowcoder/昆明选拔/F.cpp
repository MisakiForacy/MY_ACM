#include <bits/stdc++.h>
using namespace std;
using LL = long long;
int main(){
    int n;
    cin >> n;
    vector<vector<int>> mp (4,vector<int> (n+1,0));
    vector<vector<int>> dp (4,vector<int> (n+1,0));
    for (int i=1;i<=3;i++){
        for (int j=1;j<=n;j++){
            cin >> mp[i][j];
        }
    }
    for (int i=3;i>=1;i--){
        for (int j=n;j>=1;j--){
            if ()
        }
    }
}