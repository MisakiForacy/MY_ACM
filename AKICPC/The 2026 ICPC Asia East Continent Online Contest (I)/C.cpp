#include <bits/stdc++.h>

using namespace std;

using LL = long long;

struct node
{
    
};

void solve() {
    int n, k;
    cin >> n >> k;
    vector<LL> a(n + 1);
    for (int i = 1;i <= k;i ++) {
        int l, r;
        cin >> l >> r;
        vector<node> p;
    }
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int T = 1;
    cin >> T;
    while (T --) solve();
}