#include <bits/stdc++.h>

#define all(x) begin(x),end(x)

#define siz(x) ((int)x.size())

using namespace std;

using LL = long long;

struct node
{
    char op;
    int x;
};

void solve() {
    int n;
    cin >> n;
    map<int, int> have;
    vector<node> p;
    for (int i = 1;i <= n;i ++) {
        p.push_back({})
    }
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int T = 1;
    cin >> T;
    while (T --) solve();
}