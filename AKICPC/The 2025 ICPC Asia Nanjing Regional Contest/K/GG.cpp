#include <bits/stdc++.h>
using namespace std;
using LL = long long;

struct node {

};

void solve () {
    int n;
    cin >> n;
    vector <LL> v(n), l(n);
    for (int i = 0;i < n;i ++) cin >> v[i];
    for (int i = 0;i < n;i ++) cin >> l[i];
    int q;
    cin >> q;
    
}

int main () {
    ios::sync_with_stdio(0), cin.tie(0);
    int T = 1;
    cin >> T;
    while (T --) solve ();
}