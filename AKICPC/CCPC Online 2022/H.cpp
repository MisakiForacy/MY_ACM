#include <bits/stdc++.h>

using namespace std;

using LL = long long;

void solve() {
    LL L, R, K;
    cin >> L >> R >> K;
    for (LL n = 0;n <= 60;n ++) {
        if (1LL << n > )
    }
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int T = 1;
    cin >> T;
    while (T --) solve();
}