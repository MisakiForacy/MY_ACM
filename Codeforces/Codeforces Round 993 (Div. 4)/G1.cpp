#include <bits/stdc++.h>
using namespace std;
using LL = long long;
int main(){
    int T;
    cin >> T;
    while (T--){
        int n;
        cin >> n;
        vector<int> a(n+1,0);
        for (int i=1;i<=n;i++) cin >> a[i];
        int ans = 1;
        int len = n;
        map<int,int> mp;
        for ()
        while (1){
            multiset<int> st;
            for (int i=)
        }
    }
}