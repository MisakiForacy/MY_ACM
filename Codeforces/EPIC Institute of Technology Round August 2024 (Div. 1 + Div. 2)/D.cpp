#include <bits/stdc++.h>
using namespace std;

/*
dfs_order = []

function dfs(v):
    append v to the back of dfs_order
    pick an arbitrary permutation s of children of v
    for child in s:
        dfs(child)

dfs(1)
*/

void solve(){
    int n,q;
    cin >> n >> q;
    vector<int>
}

int main(){
    int T;
    cin >> T;
    while (T--){
        solve();
    }
    return 0;
}