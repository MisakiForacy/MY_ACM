#include <bits/stdc++.h>
#define all(x) begin(x), end(x)
#define siz(x) ((int) x.size())
using namespace std;
using LL = long long;

const int N = 3e6 + 10;

struct node
{
    int end = 0;
    vector<int> son(26, 0);
};

vector<node> f(3e6 + 10);

int main(){
    int n, q;
    cin >> n >> q;
    auto insert = [&](string s) -> void {

    };
    for (int i = 1;i <= n;i ++){
        string s;
        cin >> s;
        
    }
}